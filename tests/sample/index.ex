# RESULT OF JOIN (node "list", preset default)
list = [1, 2, 3, [1, 2]]

# RESULT OF SPLIT (node "list", preset default)
list = [
  1,
  2,
  3,
  [1, 2]
]

# RESULT OF JOIN (node "map", preset default)
map = %{key: 1, key_two: 2, key_tree: 3}

# RESULT OF SPLIT (node "map", preset default)
map = %{
  key: 1,
  key_two: 2,
  key_tree: 3
}
